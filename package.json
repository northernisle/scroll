{
  "name": "scroll",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "(cd www && yarn install); (cd extension && yarn install); (rm -rf node_modules yarn.lock)",
    "dev:web": "cd www && yarn dev",
    "dev:ext": "cd extension && yarn dev"
  }
}
